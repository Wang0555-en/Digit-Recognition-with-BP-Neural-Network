# BP Studio 全量训练与手绘刷新说明

## 直接开始全量训练

先关闭 BP Studio，然后在项目目录双击 **Start_Full_Training.cmd**。

这个新脚本会使用 Release 程序，默认从头训练 50 轮，初始学习率 0.3。启动前自动备份已有模型和训练记录。它需要 Python，但不需要安装额外 Python 库。学习率沿用原有计划：第 11、21、31、41 轮结束后减半，从下一轮开始生效。

也可以在 PowerShell 运行：

```powershell
cd 'C:\Users\27698\Desktop\personal files\cpp group task\BP'
python tools/train_full.py --epochs 50 --lr 0.3
```

当前本地数据已核对：

| 文件 | 样本数 | 用途 |
|---|---:|---|
| MNIST/mnist_train.csv | 60,000 | 每轮全部参与梯度更新 |
| MNIST/mnist_test.csv | 10,000 | 每轮评估，不参与梯度更新 |

“全量”是把全部 60,000 张训练图像用于训练，不是把测试集也混入训练。新入口不再按样本数截断。50 轮意味着训练集被遍历 50 次，共 3,000,000 次样本更新。

读取规则保持兼容：先查项目根目录同名 CSV，没有时查 MNIST 子目录。如果根目录有旧的小数据文件，应先确认它是否会覆盖你希望使用的 MNIST 数据来源。启动脚本会打印实际路径和行数；C++ 加载器会验证列数、标签和像素范围，遇到坏数据会报错停止。CSV 每行是 `标签,784个0到255整数灰度值`，不带表头。目录中的 idx/gz 文件不由当前训练入口读取。

只检查数据路径及行数，不启动训练：

```powershell
python tools/train_full.py --check-data
```

## 从头训练与继续训练

默认全量入口从随机权重开始，不加载旧 model.bin，适合本次重新进行完整训练。

如果要从现有 model.bin 继续训练，例如再训练 10 轮：

```powershell
python tools/train_full.py --epochs 10 --lr 0.03 --resume
```

`--resume` 只恢复权重，轮数和学习率计划重新开始；不是自动恢复到中断时的精确轮次。上面的 0.03 是可调整的示例，不保证适合所有模型。继续训练时避免直接用较大的初始学习率破坏已有权重。

旧的 Start_Training.cmd 仍打开训练/预测菜单：选 1 后使用已有模型继续训练（不存在模型时随机初始化），默认 50 轮，最多 60,000/10,000 张。推荐使用新脚本，它会备份且明确区分训练模式。不要通过注释 main 的桌面入口切换模式；双击和 F5 已恢复正常打开桌面。

## 进度、保存和中断

- 每处理 1,000 张会输出 `Epoch ... Samples .../60000`。一轮末尾还会评估 10,000 张测试图像，因此最后一个样本进度后仍需等待。
- 每轮输出 Loss（平均平方误差之和）和 Test Acc（测试集直接输入网络的准确率），并写入 training_history.csv。它不是鼠标手绘图片的准确率。
- training_dataset.csv 记录第一轮实际使用的训练样本，不会把 50 轮重复写 50 遍；全量入口每轮打乱训练顺序，故导出行序可能与源文件不同。
- model.checkpoint.bin 在每轮完成后更新；model.bin 在全部轮次完成后更新。先写临时文件，再重新加载校验，最后替换目标，减少写入中断损坏正式模型的风险。
- training_backups/时间戳/ 保存启动前的 model.bin、已有检查点、CSV 训练记录，以及本次参数 run.json。备份失败就不会开始训练。
- 首轮可能比后续轮次慢，因为要导出 60,000 行训练数据。当前是 C++ CPU BP 实现，不使用显卡；内存需要容纳全部数据和网络计算。耗时取决于机器，本次未实测完整 50 轮，不能承诺完成时间。
- 中途关闭终端或按 Ctrl+C 会终止训练，当前未完成轮次会丢失。只有本次至少完成一轮后才有本次检查点；恢复前核对检查点修改时间，避免误用上次遗留文件。

如需恢复本次最后完成轮次的权重，先另存当前 model.bin，再把确认属于本次训练的 model.checkpoint.bin 复制成 model.bin，然后运行 `--resume`。要精确延续学习率，应从 training_history.csv 确认并手工指定 `--lr`。

直接使用 `BP.exe --train-full 50 0.3` 也能训练，但它不执行 Python 启动脚本的备份步骤，因此通常使用 Start_Full_Training.cmd 或 tools/train_full.py。

## 训练结束后使用与评价

看到“Training complete”后，双击 Start_BP.cmd 重新打开程序，新窗口会加载新的 model.bin；已打开的窗口不会自动重新加载。

完整测试集评估：

```powershell
.\bin\x64\Release\BP.exe --evaluate 10000
```

应同时用自己的鼠标手写或图片样本验证实际效果。增加训练数据不保证所有手绘输入都变好，图片分割、笔画粗细、输入分布仍会影响结果。上次增加的低分拒识仍然生效，不能把减少“?”本身当成准确率提高。

## 本次手绘刷新修复与验证

DesktopUI.cpp 原先在屏幕 DC 上先填背景再绘制图像，鼠标移动时不断使整幅画布无效，中间背景帧会造成闪烁。现在背景、图像、提示文字先绘制到内存位图，再通过 BitBlt 一次提交；新增笔画只标记局部刷新范围，相同坐标的鼠标事件不触发重复绘制。修改处写有中文原因注释。

内存 DC 的绘制方式参考微软文档：https://learn.microsoft.com/en-us/windows/win32/gdi/memory-device-contexts

已完成 Release/Debug 构建；通过原训练测试、全量入口小样本测试、拒识测试和长串测试。实际打开桌面验证了多笔拖动、撤销、识别、识别后继续书写及旧结果清除。截图验证不等于高速录像，未量化刷新帧率或短暂闪烁。

本次没有启动正式全量训练，也没有替换项目现有模型。测试训练均在临时目录进行。
